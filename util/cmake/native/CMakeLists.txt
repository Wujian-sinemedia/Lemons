if (APPLE)
    add_subdirectory (Apple)
else()
    set (CMAKE_INSTALL_RPATH $ORIGIN CACHE INTERNAL "")

    if (WIN32)
        include (Windows.cmake)
    else()
        include (Linux.cmake)
    endif()
endif()
