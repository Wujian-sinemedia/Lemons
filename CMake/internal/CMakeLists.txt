include (resourcesHelpers.cmake)
include (configureHelpers.cmake)
include (directoryHelpers.cmake)

if (APPLE)
	include (native/Apple.cmake)
	setDefaultAppleSettings()
elseif (WIN32)
	include (native/Windows.cmake)
	setDefaultWindowsSettings()
endif()